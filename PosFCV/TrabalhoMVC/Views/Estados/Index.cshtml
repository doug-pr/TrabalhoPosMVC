@model IEnumerable<TrabalhoMVC.Models.Estado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Cadastrar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Codigo }, new { @class = "btn btn-primary"} ) 
            @Html.ActionLink("Detalhes", "Details", new { id=item.Codigo }, new { @class = "btn btn-info"} ) 
            @Html.ActionLink("Apagar", "Delete", new { id=item.Codigo }, new { @class = "btn btn-danger"} )
        </td>
    </tr>
}

</table>
